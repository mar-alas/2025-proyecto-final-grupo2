apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f ../docker-compose.yml
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: gestor-proveedores
  name: gestor-proveedores
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: gestor-proveedores
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f ../docker-compose.yml
        kompose.version: 1.35.0 (HEAD)
      labels:
        io.kompose.service: gestor-proveedores
    spec:
      containers:
        - env:
            - name: DB_HOST
              value: compras_servicio_db
            - name: DB_NAME
              value: compras_servicio_db
            - name: DB_PASSWORD
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: postgres
          image: gestor-proveedores
          name: gestor-proveedores
          ports:
            - containerPort: 3003
              protocol: TCP
      restartPolicy: Always
